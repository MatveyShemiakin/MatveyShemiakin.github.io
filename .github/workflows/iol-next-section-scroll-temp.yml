name: IOL next-section scroll TDD

on:
  push:
    branches:
      - fix/iol-next-section-scroll
    paths:
      - 'patients/iol-dislocation/script.js'
      - 'tests/*.test.mjs'
      - 'scripts/*.py'
      - '.github/workflows/iol-next-section-scroll-temp.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Generate published pages
        run: |
          python scripts/inject_legal.py
          python scripts/inject_site_theme.py
      - name: Check script syntax
        run: node --check patients/iol-dislocation/script.js
      - name: Run regression suite
        run: |
          for test in tests/*.test.mjs; do
            node --test "$test"
          done
