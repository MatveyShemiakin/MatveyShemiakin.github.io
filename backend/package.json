{
  "name": "clinical-navigator-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --check src/handler.mjs && node --check src/extract-clinical-facts.mjs && node --check src/providers.mjs"
  }
}
